import styled from 'styled-components';

const Wrapper = styled.div`
    display: flex;
    gap: 10px;
    margin-bottom: 12px;
    .star-container {
        svg {
            font-size: 20px;
            color: rgb(255, 185, 0);
        }
    }
`;

export default Wrapper;
